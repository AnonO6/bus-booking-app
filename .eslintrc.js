module.exports = {
  root: true,
  extends: '@react-native',
  rules: {
    'indent': ['error', 2],        // Enforce 2-space indentation
    'quotes': ['error', 'single'], // Enforce single quotes
    'semi': ['error', 'never'],     // Avoid unnecessary semicolons
  },
};